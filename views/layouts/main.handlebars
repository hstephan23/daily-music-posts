<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Daily Music</title>
</head>
<body>
    <header>
    {{#if logged_in}}
    <div class="row mt-4">
  <div class="col-md-6">
    <h3>Create a Music Post:</h3>

    <form class="form new-post-form">
      <div class="form-group">
        <label for="song-name">Song Title:</label>
        <input class="form-input" type="text" id="song-name" name="song-name" />
      </div>
      <div class="form-group">
        <label for="artist-name">Artist:</label>
        <textarea class="form-input" id="artist-name" name="artist-name"></textarea>
      </div>
      <div class="form-group">
        <label for="genre">Genre:</label>
        <textarea class="form-input" id="genre" name="genre"></textarea>
      </div>
      <div class="form-group">
        <button type="submit" class="btn btn-primary">Create</button>
      </div>
    </form>
  </div>
    {{!-- <button id="logout">Logout</button> --}}
    <a href="/logout">Logout</a>
    {{else}}
    <a href="/login">Login</a>
    {{/if}}
    </header>
    {{{ body }}}


    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    <script src="/js/post.js"></script>
    {{/if}}
</body>
</html>